import styles, { layout } from "../style";

const TestimonialsTailwind = {
    section: `${styles.paddingY} ${styles.flexCenter} flex-col relative`,
    gradient: `absolute z-[0] w-[60%] h-[60%] -right-[50%] rounded-full blue__gradient bottom-40`,
    content: `w-full flex justify-between items-center md:flex-row flex-col sm:mb-16 mb-6 relative z-[1]`,
    card: `flex flex-wrap sm:justify-start justify-center w-full feedback-container relative z-[1]`

}

export default TestimonialsTailwind