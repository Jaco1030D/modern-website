import styles, { layout } from "../style";

const CTAT = {
    main: `${styles.flexCenter} ${styles.marginY} ${styles.padding} sm:flex-row flex-col bg-black-gradient-2 rounded-[20px] box-shadow`
}

export default CTAT